# Iteration 08 (v8 — FINAL)

**Author:** Claude (refinement pass 8 of 8 — the consolidation, coverage-proof & QA pass) · **Date:** 2026-05-30

## Summary

Final holistic pass: **consolidation, coverage-proof, and QA** over the entire tree, building on v7. Per the mandate this prioritized **validation and consistency over new scope** — the only structural change is **1 dependency edge** (a risk→dep promotion) and **1 new index block** (the coverage matrix). All index blocks A–F were regenerated **programmatically from the leaf bodies** and diffed (the regen-and-diff that iteration-07 open question #1 said was still owed); drift was fixed with the **leaf as the single source of truth**. The four deliverables:

1. **Index-block reconciliation (regen-and-diff, executed).** Parsed every leaf's `_perf:_`/`_benchmark-by:_`/`GF-*`/`_deps:_` and diffed against blocks A/B/C/E:
   - **Block A (perf):** matches exactly — the 14 leaves carrying `_perf:_` are precisely the 14 table rows; the HYP/SLO split is consistent (HYP ⇔ carries `_benchmark-by:_` = 8 leaves; SLO ⇔ doesn't = 6). **No fix needed.**
   - **Block B (flags): DRIFT FOUND + FIXED.** The catalog named 10 flag keys with owning leaves, but **8 owning leaves cited no key at all** (and the consistency rule says "reference it from the owning leaf"). Added a new `_flag:_ <key> (block B)` legend sub-field and tagged all 10 owning leaves (AUT-5.2, SRC-1.7, SRC-4.1, SRC-5.1, SRC-6.1, SRC-7.1, CAT-4.1, NTF-3.1, NTF-4.1, IOS-1.1) so leaf and catalog now agree.
   - **Block C (fixtures): minor DRIFT FIXED.** Two fixtures' "primary leaves" lists omitted leaves that genuinely exercise them: `GF-DEDUP` += ENR-3.4 + SRC-1.7; `GF-TRUESPEND` += RPT-2.2. (Every claimed primary leaf already cited its fixture — verified.)
   - **Block E (build-order/waves): verified.** The W0–W5 waves, the 12-PR linear slice, the critical path, and the longest absolute chain are all **real edge chains** with no leaf scheduled before a blocker (topo-checked against the graph).
   - **Header narrative ordering: DRIFT FIXED.** The v6/v7 header asserted an "auth chain `SEC-1.1 → SEC-1.2 → AUT-3.1`," but `AUT-3.1` deps `SEC-1.1` **directly, not** `SEC-1.2` — not a real edge. Corrected to `SEC-1.1 → {SEC-1.2, AUT-3.1}` (both run in parallel after SEC-1.1). This closes iteration-07 self-critique #3 (validate ALL header ordering claims, not just the two v7 fixed). The other header chains (parallel foundation `PLT-2.1→SEC-3.5/SRC-1.2`, critical path, longest chain) were re-validated and ARE real edges.

2. **Coverage proof — new index-block F (vision→leaf matrix).** Added a terse table mapping **every README §1 vision bullet + every north-star question** to the leaf IDs that satisfy it (regenerated by keyword-scanning leaf bodies, then curated to the owning group). **Result: no holes** — all 12 vision bullets and all 7 north-star questions have ≥1 dedicated leaf, most an entire epic/feature-group. Nothing needed to be added to fill a gap.

3. **Risk→dep decision (1 promotion).** Audited all 12 risk lines that cite another leaf in their mitigation, testing each for a real ordering constraint AND cycle-safety:
   - **PROMOTED `SEC-3.1 → SEC-4.1`** (account-deletion now hard-deps the append-only audit log). SEC-3.1's own `_risk:_` says "write a SEC-4.1 audit row **before** deleting" — that is a true ordering constraint for an irreversible destructive leaf, and the edge is acyclic. (SEC-3.5 and SEC-3.2 were already hard deps; SEC-3.1 now deps all three of its named safeguards.)
   - **KEPT ADVISORY (5, would cycle):** `SEC-3.1→AUT-4.2`, `ENR-3.1→ENR-3.2`, `ENR-3.1→ENR-3.3`, `LDG-4.3→LDG-4.4` — in each the cited mitigation is a downstream/UI leaf that **already deps the destructive leaf** (e.g. the step-up UI AUT-4.2 is built *for* SEC-3.1), so reversing it would create a cycle; the ordering is correctly enforced the other way (the guard gates the *call site*). Documented this reasoning inline on SEC-3.1.
   - **KEPT ADVISORY (rest, not hard constraints):** `CAT-1.4→CAT-5.1` ("CAT-5.1-**style** history" — descriptive, not "must use CAT-5.1"), and the fallback references (`SRC-6.1/7.1→SRC-7.2`, `SRC-5.1→SRC-5.4`, `SRC-1.7→SRC-1.5`, `PLT-9.1→{SEC-4.2,CAT-4.5,ING-5.2}` which is a "this leaf gates those" forward note, `AUT-1.4→SEC-1.1` covered-by note).

4. **Scannability — tightened the 5 combo fixtures (~40% shorter).** Per iteration-07 self-critique #5 the combo fixtures had grown to a wall of prose. Trimmed redundant framing ("closes iteration-XX self-critique…", duplicated "Proves…" restatements, and the "(Wired: …)" lists that duplicate block C's primary-leaves column) while **preserving every invariant (a)–(f) and every number**: `GF-COMBO-FX` 2535→1533 chars, `GF-COMBO-STACK` 1449→796, `GF-DEDUP-XPROV` 1433→926, `GF-COMBO-DUPXFER` 969→618, `GF-COMBO-SRR` 865→609. The block-C one-liner catalog already carries the terse index, so no information was lost.

## Stats

- Leaves: **346 → 346** (no add/remove/split/merge — pure consolidation). Epics: 17 (unchanged). Feature groups: **90 by ID-prefix** (91 `### EPIC-N` subsection headers — the extra is `### RPT-8 Saved & export`, whose leaves keep their original `RPT-7.1`/`RPT-7.2` IDs per the stable-ID invariant; a pre-existing v7 artifact, not a regression).
- Size distribution: **S 95 · M 251 · L 0** — nothing exceeds `M`.
- Status mix: **27 `[built]` · 38 `[partial]` · 281 `[new]`** (unchanged).
- Dependency graph (verified programmatically): **346 nodes, 361 edges, 0 dangling, 0 self-loops, 0 cycles.** (+1 edge: `SEC-3.1 → SEC-4.1`.)
- Golden fixtures: **21** (unchanged count; 5 combo defs tightened; 2 catalog rows extended).
- `_risk:_` leaves: **37** (unchanged; SEC-3.1's mitigation promoted from advisory text to a hard dep).
- `_flag:_` (new field): **10 leaves** (every block-B owning leaf) — 1 before (only SRC-1.7 named its key inline).
- `_benchmark-by:_`: **8 leaves** (every HYP perf budget) — unchanged.
- `research` spikes with `_cost:_` + `_decide-by:_`: **12/12** preserved.
- All **346 v7 IDs preserved** (verified against the v7 snapshot); no deletions, renumbering, merges, or splits.
- Every in-line `obs:<signal>` ∈ the PLT-4.6 contract (verified: 15 distinct signals used, 0 off-contract).
- File length: 1096 → **1124 lines** (+28: block F matrix + the regen/consistency notes, partially offset by tightening the combo fixtures).

## Index-block discrepancies fixed (regen-and-diff result)

| Block | Discrepancy found | Fix (leaf wins) |
|-------|-------------------|-----------------|
| A (perf) | none | — (14 `_perf:_` leaves == 14 rows; HYP/SLO split matches `_benchmark-by:_` presence) |
| B (flags) | 8 of 10 owning leaves cited **no** flag key | added `_flag:_ <key> (block B)` to all 10 owning leaves |
| C (fixtures) | `GF-DEDUP` & `GF-TRUESPEND` primary-leaf lists omitted leaves that exercise them | `GF-DEDUP` += ENR-3.4/SRC-1.7; `GF-TRUESPEND` += RPT-2.2 |
| D (file paths) | none | — |
| E (waves/slice/critical-path) | none (all real edge chains, topo-verified) | — |
| Header "auth chain" | `SEC-1.1→SEC-1.2→AUT-3.1` implied AUT-3.1 deps SEC-1.2 (NOT a real edge) | corrected to `SEC-1.1 → {SEC-1.2, AUT-3.1}` (parallel) |

## Coverage-matrix result

Block F (new). Every §1 vision bullet + north-star question → ≥1 leaf. **No holes found, no leaf added to fill a gap.** Each bullet maps to a dedicated owner group: true-spend→LDG-2, receivables→OWE-1/2, returns→OWE-3, receipts/line-items→RCP, dedup→ENR-2/3, LLM-learns→CAT-4/5, prediction/anomaly→INT, fragmented-money→SRC, review-queue→REV, web+iOS→NAV+IOS, sign-in→AUT-1+SEC-1.1, push→NTF. North-stars: where-money-went→RPT-2/3, came-from→RPT-4.3+LDG, true-spend→LDG-2.4, owe/owed→OWE, what-changed→INT-4+RPT-4, state-now→RPT-1+LDG-3.

## Risk→dep promotions

| Leaf | Risk mitigation that became a hard dep | Why promoted / why others kept advisory |
|------|----------------------------------------|------------------------------------------|
| **SEC-3.1** | **`SEC-4.1`** (audit log) | risk says "write a SEC-4.1 audit row BEFORE deleting" — a real ordering constraint for irreversible erasure; acyclic. SEC-3.5/SEC-3.2 already deps. |
| SEC-3.1 | AUT-4.2 (step-up) — **kept advisory** | AUT-4.2 already deps SEC-3.1 (step-up UI built *for* the flow); reversing would cycle. Enforced the other way (guards the call site). |
| ENR-3.1 | ENR-3.2/3.3 — **kept advisory** | both are downstream UI/unmerge leaves that already dep ENR-3.1; reversing would cycle. |
| LDG-4.3 | LDG-4.4 — **kept advisory** | LDG-4.4 (read cutover) already deps LDG-4.3 (the backfill); the "before cutover" note is a forward reference, already a real edge the other way. |
| CAT-1.4 | CAT-5.1 — **kept advisory** | "CAT-5.1-**style** history" is descriptive, not a hard "must use CAT-5.1" coupling. |

## FINAL QA checklist (run programmatically — pass/fail each)

| # | Check | Result |
|---|-------|--------|
| 1 | Unique IDs (no duplicates) | **PASS** — 346 distinct, 0 dupes |
| 2 | Acyclic dependency graph | **PASS** — 0 cycles (DFS) |
| 3 | No dangling deps (every dep target is a real leaf) | **PASS** — 0 dangling |
| 4 | No self-loops | **PASS** — 0 self-loops |
| 5 | No leaf above `M` | **PASS** — S 95 · M 251 · L 0 |
| 6 | Every leaf has `_accept:_` | **PASS** — 0 missing |
| 7 | Every leaf has `_test:_` | **PASS** — 0 missing |
| 8 | Every `obs:<signal>` ∈ PLT-4.6 contract | **PASS** — 15 distinct signals, 0 off-contract |
| 9 | Every `research` spike has `_cost:_` + `_decide-by:_` | **PASS** — 12/12 |
| 10 | Every HYP perf leaf has `_benchmark-by:_` | **PASS** — 8/8 (CAT-2.1/2.2/4.2, ING-6.1/6.2, LDG-2.5, PLT-8.2/8.4) |
| 11 | No SLO perf leaf carries `_benchmark-by:_` | **PASS** — 6 SLO leaves, 0 carry it |
| 12 | Every vision bullet + north-star covered | **PASS** — block F, no holes |
| 13 | All v7 IDs preserved (no silent drop) | **PASS** — 0 v7 IDs missing, 0 new IDs |
| 14 | Index block A == leaves (perf rows) | **PASS** — 14 == 14, HYP/SLO consistent |
| 15 | Index block B owning leaves cite their flag | **PASS** — 10/10 via `_flag:_` |
| 16 | Index block C primary leaves cite their fixture | **PASS** — every claimed primary leaf cites its GF |
| 17 | Index block E waves/slice/critical-path are real edge chains | **PASS** — no leaf precedes a blocker |
| 18 | Header ordering claims are real edges | **PASS** — auth-chain corrected; rest verified |

**18/18 PASS.**

## What changed per epic (terse)

- **SEC** — `SEC-3.1` gained the `SEC-4.1` hard dep (risk→dep promotion) + inline reasoning for why AUT-4.2 stays advisory.
- **AUT/SRC/CAT/NTF/IOS** — `_flag:_ <key>` tag added to the 10 block-B owning leaves (AUT-5.2, SRC-1.7, SRC-4.1, SRC-5.1, SRC-6.1, SRC-7.1, CAT-4.1, NTF-3.1, NTF-4.1, IOS-1.1).
- **Header / index blocks** — version → **v8 (final)**; `_deps:_` & `_flag:_` legend notes updated; consistency-rule block records the executed regen-and-diff; auth-chain narrative corrected; block C two rows extended; **block F coverage matrix added**; the 5 combo golden-fixture definitions tightened (~40% shorter, all invariants kept); footer rewritten as "dispatch-ready."
- **All other epics** — no leaf-body change; covered by the regen, the coverage matrix, and the unchanged SLO budgets.

## Splits / merges (old → new IDs)

- **None.** No splits, no merges, no deletions, no renumbering, no net leaf change (346 → 346). The only structural delta is the 1 dependency edge `SEC-3.1 → SEC-4.1`.

## Decisions

- **Index blocks are now actually regenerated, not hand-asserted.** v7 added blocks/columns by hand; v8 parsed the leaves and diffed (closing iteration-07 open question #1). The found drift (block B flags, block C two rows, the header auth-chain) was all fixed with the leaf winning — proving the consistency rule works as a real verification step, not just a stated policy.
- **One risk→dep promotion, conservatively chosen.** Only `SEC-3.1→SEC-4.1` is a genuine "destructive leaf must follow its safeguard" constraint that is also acyclic. The rest of the cited mitigations are either already-real edges in the reverse direction (would cycle) or descriptive/fallback references — promoting them would over-couple the graph or break acyclicity. Each decision is recorded.
- **Coverage is proven by a matrix, not asserted.** Block F is the last-chance hole check; it found none, confirming the tree fully covers the §1 vision and all north-star questions.
- **Tightening over growth.** The combo fixtures were the main scannability cost; they were compressed without dropping a single invariant, and the duplicated "(Wired:)" lists were removed in favor of block C's catalog as the single index.

## State of the backlog (closing note)

**The backlog is dispatch-ready.** It is internally consistent (18/18 QA checks pass), fully covers the product vision (block F, no holes), and has a **topo-verified dispatch order** an implementer can follow without re-deriving dependencies: hand **wave W0** from index-block E to agents (PLT-2.1, PLT-2.3, SEC-1.1, SEC-1.3, PLT-9.1, PLT-4.1, PLT-3.1 — all dep-free or partial-foundation), then W1→W5 to the deduped-true-spend wedge, with the flattened **first-12-PR linear slice** as the single-threaded path. Every leaf reads as a self-contained brief (`_accept:_`/`_test:_`/`_data:_`/`_files:_`, with `_deps:_`/`_perf:_`/`_benchmark-by:_`/`_risk:_`/`_flag:_` where applicable). The one genuinely external unknown the tree cannot resolve internally remains Gmail CASA lead-time/cost (`SRC-6.1`), already encoded as a default-OFF flag + the SRC-7.2 forward-to-email fallback. Beyond the W0–W5 wedge spine, downstream leaves are ordered by their individual `_deps:_` (block E is intentionally a wedge-first starter spine, stated as such).

## Invariants honored

IDs stable; **no leaf deleted/added/renumbered/split/merged** (346 → 346); coverage preserved AND proven (block F); server-verified identity untouched (SEC-1.1/1.2 unchanged; the SEC-3.1→SEC-4.1 promotion *reinforces* erasure safety); encrypted tokens untouched; migration-safe + idempotent data leaves still `_xc: idem`; money on integer minor units preserved (combo-FX fixture still pins native-currency split math, non-destructive conversion); staging/prod separation untouched; provider-abstraction + Plaid-default-swappable intact; the 1 new edge keeps the graph acyclic. **Verified programmatically: 346 leaves, 0 duplicate IDs, 0 dangling deps, 0 cycles, 0 self-loops, 0 leaves above `M`, 361 edges, 12/12 spikes carry cost+decide-by, 8/8 HYP perf leaves carry benchmark-by, 0 SLO leaves carry benchmark-by, 10/10 flag-owner leaves cite their key, all 346 v7 IDs present, every in-line `obs:<signal>` ∈ the PLT-4.6 contract, blocks A/B/C/E reconciled against the leaves with the leaf winning, the corrected critical path + first-12 slice + W0–W5 waves are fully-linked real edge chains, every §1 vision bullet + north-star question maps to ≥1 leaf.**

---

_End of the 8-pass iterative build. v8 is the canonical, dispatch-ready backlog._
