// The e2e user lives in the Clerk DEVELOPMENT instance only. It is created
// by global.setup.ts through the Clerk Backend API (no password — the
// instance is Google-only; tests sign in with a single-use sign-in ticket).
export const E2E_USER_EMAIL = "cashlens-e2e@example.com";
