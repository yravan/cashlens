# Shipped functionality verification

Audit started 2026-09-09. Baseline: `origin/main` at `c7b8ba2` (runtime unchanged
from `c2054d7`). This is a coverage and failure register, not a certification that
software is bug-free. Public evidence contains no real financial records or secrets.

## Evidence levels

- Live: actual signed-in Chrome workflow or owner-scoped read-only production comparison.
- Automated: real local PostgreSQL, Clerk development sessions, and Plaid sandbox where
  configured. External substitutes cover fault injection; application code is not mocked.
- Source: implementation/contract inspection only; does not prove a provider works live.
- Pending: unresolved or not yet verified. Future leaves are not counted as shipped defects.

## Shipped leaf matrix

Paths below are relative to `apps/web` unless otherwise noted. Automated-suite evidence
is from PR #57 (`bb66e30`, 361 API / 71 browser tests) and PR #60 (`9331b82`, 361 API /
72 browser tests), not a claim that their fixes have deployed. Both production builds,
lint/typecheck, and CI passed. Browser artifacts passed the existing scrubbing teardown.

| Leaf | Implementation | Behavioral evidence | Remaining limit |
| --- | --- | --- | --- |
| 1.1.1 Sign-in | `app/sign-in`, `app/sign-up`, `proxy.ts` | Live signed-in session; signed-out protected routes redirect; `e2e/auth.*` | Automated auth uses Clerk dev accounts, not a live Google credential exchange |
| 1.1.2 Identity/scoping | `lib/data/users.ts`, `lib/db/client.ts` | API `me`, `users-dal`; e2e `identity`, `isolation`, `ledger` | Production cross-user attack simulation not performed |
| 1.2.1 Shell/navigation | `app/(app)/layout.tsx`, `nav.tsx` | Live traversal of every shipped section; e2e `shell` desktop/phone/sign-out | Continued adversarial workflow checks pending |
| 3.1.1 Ledger model | `lib/db/schema.ts`, `lib/data/ledger.ts` | API `ledger-dal`; e2e `ledger`; minor-unit tests | Provider coverage is separate from schema correctness |
| 1.3 Deployment | `vercel.json`, deploy scripts, health route | Live health 200 with DB healthy; production headers checked | Deployment identity and migration gate inspection pending |
| 2.1.2 Credentials | `lib/crypto/credentials.ts`, `lib/data/connections.ts` | Crypto/AAD and connection DAL tests, isolation and no-leak assertions | Production ciphertext not decrypted for audit |
| 2.1.1 Connect | Plaid routes, `accounts/connect-button.tsx` | API `plaid-connect`; real sandbox e2e; prior live Link navigation | Browser completion path not directly exercised by current harness |
| 2.1.3 Backfill | `lib/data/plaid-sync.ts`, sync route | API `plaid-backfill`; live account-filter counts reconcile | Misleading completion count corrected in open PR #57 |
| 2.1.4 Sync | Sync engine, webhook verifier, resume component | API sync/webhook lifecycle, signature, race, failure tests | Fresh real production webhook delivery not induced |
| 2.1.5 Connections | Connection action DAL/routes/UI | Real sandbox repair/disconnect/purge tests; live Cancel and unchecked purge default | No destructive production actions performed |
| 6.3.1 Accounts | `accountOverview`, `accounts-overview.tsx` | All live balances and per-currency summaries reconcile; API overview/e2e ledger | Agreement with stored balances is not independent institution verification |
| 4.1.1 Categories | Taxonomy, category DAL/route/select | API categories; real browser manual assignment/persistence/isolation on fixtures | No manual changes to real categories |
| 3.6.1 History | History parser, DAL, page/filter UI | Live pagination, filter clearing, empty search, invalid period; API/unit/e2e history | Unsubmitted filter clearing still to check |
| 4.2.1 Auto-categorization | Auto-categorize DAL/route/UI, LLM client | API validation/races/provenance; e2e category backlog | Production dormant: missing configured transport; OpenRouter PR #59 is open |
| 3.3.1 Transfers | Transfer matcher, DAL/routes/UI | Numeric invariants, unlink/dismissal/races/isolation tests | Real economic intent cannot be established from amount/date alone |
| 6.1.1 Cash flow | `cashFlowSummary`, dashboard | Every live month/currency bucket and exclusion count reconciles; API/e2e | Missing cross-source/investment coverage can distort financial interpretation |
| 6.2.1 Spending | `spendingByCategory`, spending page | Live all-time/period reconciliation and drill filters; API/e2e | Stale applied-filter values corrected in open PR #60 |
| 6.4.1 Recurring | Detector, recurring DAL/page/actions | Three live samples' counts/dates/medians reconcile; fixture confirm/dismiss and mobile tests | Suggestions are not verified subscriptions; real decisions left untouched |
| 10.1 CI | Repository `.github/workflows/ci.yml` | PR checks green; GitHub main protection requires API/e2e/lint-typecheck, including admins | Separate security-scanning leaf 10.7 is absent |
| 10.2 Backend harness | `tests/harness`, Vitest config | Full local real-DB suites pass with per-worker isolation | Fault substitutes prove handling, not current provider availability |
| 10.3 Browser harness | Playwright config, `e2e` | Full real-browser suites pass, including mobile and RLS seams | Connect completion UI remains a coverage gap |
| 10.4 Seed data | `db/seed` | Seed anchors/idempotency/local-only guard tests | Fixtures are intentionally synthetic |

## Current failure register

| Item | Classification | Disposition |
| --- | --- | --- |
| Connect completion used only this tab's imported count | Confirmed shipped defect | PR #57 open; fresh GPT review pending |
| Spending Clear retained previously applied control values | Confirmed shipped defect | PR #60 open; red/green regression; fresh GPT review pending |
| Auto-categorization is dormant in production | Configuration/deployment gap | PR #59 pending; user authorizes enablement after merge |
| Negative flow interpreted as overspending despite incomplete coverage | Financial interpretation limit | Do not certify true spending from ledger reconciliation alone |
| Individual transfer pair correctness | Unverified | Earlier asserted false matches retracted; reimbursement context can matter |

## Deferred and pending work

- Human Interest: explicitly excluded by the user.
- 3.3.2 cross-source duplicate merging, 3.2.1 pending reconciliation, and 2.5 investment
  support are not shipped. The investment leaf names balances/holdings, not explicitly
  investment transaction history; that scope needs clarification, not an assumed guarantee.
- 6.4.2 upcoming expenses is in open PR #58, not the deployed baseline.
- 10.7 security scanning is named in the feature tree but not implemented in current CI.
- The user now separately requests model iteration and replacement of stale automatic
  classifications after #59 merges. Existing code only fills NULL categories. A controlled
  rerun must preserve manual decisions, replace provenance fields together, and permit rollback.
- Exact GPT review mapping requested by the user: Fable = Astra, Opus = Sol, Sonnet = Luna.
  Earlier #57/#60 reviews used actual Sonnet 5; fresh Luna reviews are underway.

## Completion gates

- [ ] Finish adversarial live workflow pass and record every confirmed failure.
- [ ] Validate each source-level concern with a focused failing test or stronger runtime evidence.
- [ ] Resolve both correction PRs through the requested GPT review cycle.
- [ ] Finish deployment/security-boundary verification without credential disclosure.
- [ ] Recheck #59 merge state, then follow the user's production activation instructions.
- [ ] Publish this sanitized matrix for review; never merge PRs automatically.
