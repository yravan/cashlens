# Shipped functionality verification

Audit started 2026-09-09. This is a coverage and failure register, not a
certification that software is bug-free. Public evidence contains no real owner
transactions or secrets.

## Current status (authoritative)

This section supersedes older readiness snapshots below. GitHub reports PRs #57,
#60, #61, #62, #63, #64, #65, #66, #67, #68, #69, #70, #71, #72, #73, #74,
#75, and #76 merged. The fetched `origin/main` is `723dbc9`; PR67 is `0f89d59`,
PR75 is `231e650`, and PR74 is `decf403`.
The previously deferred 3.2.1 pending-to-posted reconciliation claim is resolved
by #68.

Current merged-head evidence includes:

- #70: `0841221`, 437 API tests / 31 files / 29.46s, 93 browser tests / 1.8m,
  local HTTP/1, build/static checks, Sol review, and CI `34563227438`.
- #72: `7988800`, 428 API / 32 files / 26.70s, 93 browser / 1.7m, fresh
  bootstrap and upgrade preservation, build/static checks, and CI `34563655612`.
- #73: `55e5a25`, 437 API / 33 files / 28.62s, 93 browser / 1.8m, bootstrap,
  build/static checks, and CI `34563754920`; the first browser auth-session
  refresh attempt failed before assertions, and the unchanged rerun passed.
- #74: `b17a120`, 465 API / 34 files / 35.65s, 93 browser / 1.8m, fresh-DB
  migration consistency, build/static checks, Sol review, and CI `34564307265`.
  A taxonomy-lock mutation caught three failures and two passes; the mutation
  was restored and the full API suite remained green.
- #75: `70d94a1`, 99 browser / 2.4m plus 428 API / 30 files / 24.54s on the
  unchanged runtime, build/static checks, and CI `34567387196`. Deliberate
  signed-out cleanup and registered-owner-guard mutations were red and restored;
  default transport validation passed.
- #67 merged as `0f89d59`; final head `eb87ddd04ba54c8a3d8d0723b265bacd71c77a76`
  passed 492 API tests / 35 files / 44.54s, 102 browser tests / 4.6m including
  real two-minute lost-response recovery, build/static checks, a fresh DB with
  migration 0030, and Sol review. CI `34567838201` passed with default transport
  (e2e 6m6s, API 1m11s, static 31s). Vercel deployment succeeded and public
  health returned 200 for app and database.

Vercel #67, #74, and #75 are deployed successfully with public health 200 for
app and database. Native Chrome recovered an existing Cash Lens tab: Accounts
reload balances and per-currency summaries reconcile, and a stale open-page
balance corrected on reload; hidden retained DOM rows were excluded. Live Cancel
and unchecked-purge checks caused no disconnect, deletion, or financial-data
change. The in-app `node_repl` browser tool still fails before execution, so no
claim is made for that tool.

Read-only production checks find all 11 public tables `FORCE ROW LEVEL SECURITY`,
neither app nor owner role is superuser or `BYPASSRLS`, all active transfer pairs
meet posted/different-account/opposite-sign/equal-magnitude/same-currency/4-day
invariants, and credential lifecycle rows have no invalid active/disconnected or
awaiting-cleanup combinations. No incomplete auto or contaminated manual
provenance was observed. Investment accounts have zero ledger history; investment
transaction coverage remains a future 2.5 limit.

Model semantic quality remains an open defect: private inspection found wrong
semantics, including a high-confidence classification from the current model;
the prompt and resulting row are journaled, while older rows lack run provenance
needed to attribute historical requests. The current stub/schema tests and the
offline evaluator at `57a3bab` (500 API/unit tests, eight scorer tests) do not
prove real-account accuracy. No paid calls were made; the proposed $1 synthetic
evaluation awaits approval. The public document contains no owner examples,
counts, raw reasons, or merchant labels. Receipt evidence, user-confirmed
purpose, learning from corrections, and scalable grouped/bulk review remain
absent. This record makes no bug-free claim.

## Evidence levels

- Live: actual signed-in Chrome workflow or owner-scoped read-only production comparison.
- Automated: real local PostgreSQL, Clerk development sessions, and Plaid sandbox where
  configured. External substitutes cover fault injection; application code is not mocked.
- Source: implementation/contract inspection only; does not prove a provider works live.
- Pending: unresolved or not yet verified. Future leaves are not counted as shipped defects.

## Merge Authorization And Deployment

On September 10 the founder explicitly authorized this agent to start merging PRs
as they become ready, superseding the earlier no-merge instruction for this audit.
Reviews, validation, dependency ordering, and production checks still apply; the
authorization is not permission to bypass failed checks or merge unfinished work.
The merged PR list above is the current checkpoint. The historical snapshots below
retain the evidence available at each earlier point and are not current readiness
claims.

### Historical merge checkpoint (September 10, Pacific)

This checkpoint is retained for historical evidence only.

- PR71 merged as `d38415b` after independent Luna integration approval,
  417 local API and 93 browser passes, build/lint/typecheck, and green CI
  `34562605264`.
- PR76 merged as `ca930ce` after independent Luna review corrected an Apple
  field-availability overpromise. Both roadmap nodes remain under 50 words;
  research is committed, MVP scope unchanged, and CI `34562482116` green.
  These are future capabilities, not newly shipped integrations.
- PR66 merged as `367f235` after independent Sol review, 422 local API and
  93 browser passes, build/lint/typecheck, and green CI `34563066039`.
  Fresh bootstrap and an isolated upgrade from exact deployed migration 0025
  passed. Repeating migration preserved 28 journal records, synthetic data,
  all 29 policies, forced RLS, cross-user isolation, and existing grants.
  Allowed cleanup transitions succeeded; identity rewrites and reverse status
  transitions failed with SQLSTATE 42501. The disposable database was removed.
  One local browser attempt stopped at authentication during a connectivity
  interruption; the full retry passed and artifacts were scrubbed.
- Vercel reports successful deployment of `367f235`; a subsequent public health
  probe returned HTTP 200 with app and database healthy.
- PR57, PR69, and PR70 include main through `ca930ce`; required CI is green.
  This older checkpoint predates the authoritative status above. PR67 and the
  reclassification stack were being aligned with deployed cleanup migrations
  0026/0027 at that time.

- PR65 merged at `2de9decd33326592cc004961404a32e403e38638`, matching reviewed
  head `0dc7552`, with required PR checks green and GitHub reporting mergeable.
  A merge commit preserves dependency ancestry; the source branch was retained.
  Vercel reports successful deployment and a fresh public health probe returns
  HTTP 200 with app/database healthy. Post-merge CI `34503883576` has passing
  API and lint/typecheck jobs; browser CI remains in progress at this checkpoint.
- The preceding main run `34498779830` for founder-merged PR68 failed only
  browser CI. A count/marker-only log inspection confirms the known zero-import
  Plaid assertion. No raw log content or credentials were printed.
- PR68's migration is now on main. Reclassification migrations must be reconciled
  with that deployed history before their PRs are eligible to merge.

## Shipped leaf matrix (current evidence)

Paths below are relative to `apps/web` unless otherwise noted. Current merged-head
validation is 492 API tests across 35 files, 102 browser tests, fresh-DB migration
0030, build/static checks, and green CI `34567838201`. These are bounded checks,
not a bug-free certification; historical failure details remain below.

| Leaf | Implementation | Behavioral evidence | Remaining limit |
| --- | --- | --- | --- |
| 1.1.1 Sign-in | `app/sign-in`, `app/sign-up`, `proxy.ts` | Signed-in Chrome session and signed-out protected-route redirects; auth e2e | Clerk dev auth is not a live Google credential exchange |
| 1.1.2 Identity/scoping | `lib/data/users.ts`, `lib/db/client.ts` | Owner-scoped reads; 11 public tables force RLS; app/owner lack superuser and BYPASSRLS | No authenticated cross-user attack was performed in production |
| 1.2.1 Shell/navigation | `app/(app)/layout.tsx`, `nav.tsx` | Shipped-section traversal and desktop/phone/sign-out e2e | Native Chrome follow-up was bounded to recovered workflows |
| 3.1.1 Ledger model | `lib/db/schema.ts`, `lib/data/ledger.ts` | API/e2e ledger coverage and minor-unit tests | Investment accounts have zero ledger history; transaction history is future 2.5 scope |
| 1.3 Deployment | `vercel.json`, deploy scripts, health route | Vercel deployments and public app/database health 200; security headers; migration-before-build gate | No production migration was deliberately induced |
| 2.1.2 Credentials | `lib/crypto/credentials.ts`, `lib/data/connections.ts` | Crypto/AAD, isolation, no-leak, and lifecycle checks; no invalid active/disconnected or awaiting-cleanup rows | Production ciphertext was not decrypted for audit |
| 2.1.1 Connect | Plaid routes, `accounts/connect-button.tsx` | API, Sandbox, completion-race, Cancel, and preserved-connection browser coverage | No production Item was created or removed in these checks |
| 2.1.3 Backfill | `lib/data/plaid-sync.ts`, sync route | Cursor-readiness correction, account-filter reconciliation, and real Sandbox repetition in merged validation | Provider coverage remains bounded to configured Sandbox checks |
| 2.1.4 Sync | Sync engine, webhook verifier, resume component | API sync/webhook lifecycle, signature, race, failure, and resume tests | Fresh real production webhook delivery was not induced |
| 2.1.5 Connections | Connection action DAL/routes/UI | Repair/disconnect/purge Sandbox tests; native Chrome Cancel and unchecked purge caused no deletion or financial-data change | No destructive production action was performed |
| 6.3.1 Accounts | `accountOverview`, `accounts-overview.tsx` | Native Chrome reload balances and per-currency summaries reconcile; stale open-page balance corrected; hidden retained DOM filtered | Stored-balance agreement is not independent institution verification |
| 4.1.1 Categories | Taxonomy, category DAL/route/select | API, browser assignment/isolation, and taxonomy-lock concurrency coverage | No owner category was manually changed |
| 3.6.1 History | History parser, DAL, page/filter UI | Pagination, clearing, empty search, invalid-period API/unit/e2e coverage | No owner history was reclassified |
| 4.2.1 Auto-categorization | Auto-categorize DAL/route/UI, LLM client | API validation/races/provenance and bounded Chrome/read-only inspection; current model can produce wrong high-confidence semantics | No real-account accuracy proof; older rows lack run provenance; no paid calls |
| 3.3.1 Transfers | Transfer matcher, DAL/routes/UI | All active pairs satisfy posted/different-account/opposite-sign/equal-magnitude/same-currency/4-day invariants | Pair invariants do not prove economic intent |
| 6.1.1 Cash flow | `cashFlowSummary`, dashboard | API/e2e flow and transfer exclusion reconciliation | No owner counts or cross-source/investment accuracy claim |
| 6.2.1 Spending | `spendingByCategory`, spending page | All-time/period reconciliation, drill filters, and Clear regressions | No owner category-purpose validation |
| 6.4.1 Recurring | Detector, recurring DAL/page/actions | Detection cadence, dates, medians, confirm/dismiss, and mobile tests | Suggestions are not verified subscriptions; no real decisions changed |
| 6.4.2 Upcoming | `lib/ledger/upcoming.ts`, recurring DAL, upcoming page | Calendar/list date arithmetic and short-month correction in merged validation | Projected payment intent is unverified |
| 10.1 CI | Repository `.github/workflows/ci.yml` | Required API/e2e/lint-typecheck protection; CI `34567838201` green | Security-scanning leaf 10.7 remains absent |
| 10.2 Backend harness | `tests/harness`, Vitest config | 492 API tests / 35 files on final merged head; real DB/RLS worker isolation | Offline evaluator 500 API/unit tests do not prove model accuracy |
| 10.3 Browser harness | Playwright config, `e2e` | 102 browser tests / 4.6m, including two-minute lost-response recovery; artifacts scrubbed | In-app `node_repl` browser tool remains unavailable |
| 10.4 Seed data | `db/seed` | Seed anchors, idempotency, local-only guard, and fresh migration checks | Fixtures are synthetic and contain no owner examples |

## Historical failure register and unresolved limits

| Item | Classification | Disposition |
| --- | --- | --- |
| Connect completion used only this tab's imported count | Confirmed shipped defect | PR #57 at `367fff6`; real browser regression red/green; 361 API / 72 browser local pass; fresh Luna review approves; CI fails a separate older sandbox case |
| Historical completion accepted before Plaid provides a usable sync cursor | Confirmed shipped defect | PR #63 at `24a417e` ready for founder review: fresh/legacy state regressions red/green, valid empty history covered, full local and CI green, independent Luna approval |
| Spending Clear retained applied or unsubmitted draft values | Confirmed shipped defect | PR #60 expanded at `26c2a73`; both tests red/green; 361 API / 73 browser pass; fresh Luna review approves; CI green after one failed-job rerun |
| Transactions Clear retained unsubmitted draft values | Confirmed shipped defect | PR #62 at `2249bff`; test red/green; 361 API / 72 browser pass; fresh Luna review approves; CI green |
| Auto-categorization was dormant in production | Resolved transport gap | PR #59 merged and deployed; user-authorized live browser visit successfully classified a bounded initial sample. No production credential changes were needed |
| Negative flow interpreted as overspending despite incomplete coverage | Financial interpretation limit | Do not certify true spending from ledger reconciliation alone |
| Individual transfer pair correctness | Unverified | Earlier asserted false matches retracted; reimbursement context can matter |
| Account retrieval fails after token exchange, leaving an active Item without a durable credential | Confirmed shipped defect | Draft server PR #66 and client PR #67 stack on #65. Fresh Sol approves server `ec66594`; fresh post-push Luna approves updated client `a00e8fc`. Updated client passes 394 API tests (88.86s), build/lint/typecheck, and CI `34467224609` all checks. Local full browser: 91 passes, one existing spending assertion failure; both added cleanup regressions pass. That race is corrected separately by ready PR #71; integrate it and #63 before final stack validation. Server doc-head CI `34457252672` hit known #63 readiness defect (0 imported; 88 passes). Delayed retry now has real two-minute browser coverage: first actual cleanup returns 200, browser acknowledgement is dropped, retry returns 404, connection is disconnected and credential gone. A one-attempt mutation produced the intended RED and was restored. No blind full-suite rerun or production changes |
| Duplicate cancellation exchanged a token solely to remove its Item | Covered by pending correction | PR #66 API tests assert abandon compatibility is a no-op with no exchange/provider state, and same-Item rejection never revokes the working connection, including concurrent exchanges. PR #67 now browser-tests Cancel against a synthetic preserved connection: no exchange/abandon request, unchanged account/credential/status counts, Connected after reload. Restoring the abandon POST produced the intended RED. The deliberately non-decryptable fixture proves preservation, not provider usability; the correction is not deployed |
| Spending uncategorized drill test assumes rows remain unclassified after navigation | Confirmed test-ordering defect; PR #71 ready | PR #67's full browser trace showed categorization HTTP 200 with five assignments and zero remaining candidates; the expected eight USD rows became four. PR #71 at `1a6a09e`, stacked on #65, waits for the real transfer-match refresh before clicking, then real categorization completion, and asserts canonical remaining row identities/count. Independent review caught and corrected the missing transfer-match prerequisite. Disabling the actual uncategorized filter produced 17 rows instead of four and the intended RED. Restored full validation: 388 API tests (90.88s), 89 browser tests (3.2m), build/lint/typecheck pass; fresh post-PR Luna approval; CI `34468301833` all green. An initial Clerk setup fetch failure was retained and connectivity verified before retry. No runtime or production change |
| Concurrent requests for one unknown webhook key duplicate provider lookups | Confirmed defect; PR #70 ready for founder review | The original pure probe observed 20 provider calls for 20 same-key invalid JWTs. PR #70 at `5d04f39`, stacked on #65, coalesces calls and bounds cache size, starts, concurrency, and transport lifetime. Final protected-base validation: 408 API tests in 30 files (82.50s), 89 browser tests (3.9m), production build/lint/typecheck pass, fresh post-PR Sol review approves, CI `34463639574` all green. Four deliberate mutations were caught and restored; the 19-test lookup suite includes actual Plaid/Axios loopback cancellation. Main-based API validation separately passed 410 tests. Browser tests use Clerk development/Sandbox, never the production ledger. No merge/deployment performed; #64 separately fixes body admission. This is not an authentication bypass or proof of exploitation |
| Webhook body limit is checked after `request.text()` | Confirmed shipped resource-boundary defect | PR #64 at `1d3c63b`: normal API harness reproduced full 1 MiB consumption before the 256 KiB rejection; broken-stream error also red/green. Full local 395 API tests pass with one worker, 77 browser tests pass, build/lint/typecheck pass. Default parallel API run encountered local database failures. Independent Luna review approves; CI `34452897544` all green |
| Overlapping categorization requests send the same candidate to the model twice | Confirmed local resource-control defect; PR #73 ready | A synthetic one-row fixture exercised the actual categorize POST and DAL against real local Postgres and the loopback OpenRouter substitute. Holding the first model response while issuing a second same-user POST produced two model calls; both requests returned 200. The write guard prevented a second classification write, but did not prevent duplicate provider work. No production rows or real model credentials were used. PR #73 now implements tested server-side admission, with independent Sol approval and final-head CI green; it is not deployed. The provider key's finite spending limit is not an application-side quota |
| Upcoming loses its original monthly day after a short-month clamp | Confirmed shipped projection defect | Draft PR #69 at `a8130c5`, stacked on #65. January 31 -> February 28 -> March 28 prematurely marked the stream stale on March 29. Pure, API, and browser regressions observed RED/GREEN; 61 pure cases cover 29/30/31 and annual leap-day anchors. Main-based full validation: 396 API / 78 browser pass. Stacked validation: 394 API pass, 89 browser pass with one known #63 zero-import failure; calendar regression passes. Build/lint/typecheck, fresh independent Luna review, and CI `34459213587` pass. Keep draft pending #63 dependency validation; no production data modified |
| Clerk testing retry diagnostics can include token-bearing URLs in stdout | Confirmed local test-log boundary defect | PR #65 at `0dc7552` ready: shared live/artifact redaction, 16 focused tests, 20 cancellation repeats, 388 API / 89 browser tests, build/lint/typecheck, independent Luna approval, CI `34454365797` all green. Count-only scan of that CI log found zero dev-browser-token or JWT-shaped occurrences; this is not proof against every possible secret format |
| Plaid browser tests delete local credentials without removing their Sandbox Items | Confirmed test-harness cleanup defect; draft PR #75 | Independent Luna source audit of integration `9db9ba3` confirms three affected exchanged-Item workflows, including #57's completion race. PR #75 at `deb4840`, based on #71, adds authenticated provider-first per-test disconnect and a raw-cleanup credential guard. Full API: 388 pass (89.51s); focused Sandbox browser: 6 pass (47.6s); removing the guard produced the intended RED and was restored. Full browser: 88 pass, two failures (2.1m): local hardcoded health port, corrected temporarily with 4/4 focused passes, and the known zero-import backfill race addressed by #63. Temporary port edits restored; build/lint/typecheck and artifact scrub pass. Fresh Luna review, CI `34481744166`, and integrated validation pending. No production Item was touched; this establishes a provider resource leak, not Sandbox billing |

## Deferred and pending work

- Human Interest: explicitly excluded by the user.
- 3.3.2 cross-source duplicate merging and 2.5 investment support are not shipped.
  The investment leaf names balances/holdings, not explicitly investment transaction
  history; that scope needs clarification, not an assumed guarantee. 3.2.1 is shipped
  with #68 and is not deferred.
- 6.4.2 upcoming expenses merged during this audit; new behavior is being verified separately.
- 10.7 security scanning is named in the feature tree but not implemented in current CI.
- The user now separately requests model iteration and replacement of stale automatic
  classifications after #59 merges. Existing code only fills NULL categories. A controlled
  rerun must preserve manual decisions, replace provenance fields together, and permit rollback.
  Ready PR #72 adds the 4.2.4 journal foundation: forced-RLS owner-bound runs
  and proposals, immutable before-images, model fingerprints, revision fields, and exact
  microsecond timestamps. Review found missing run-kind and proposal/applied-value
  constraints, now corrected. Initial local PostgreSQL execution caught fixture SQL,
  timezone-format, and retained-run count errors. Six deliberate mutations covering
  run-kind, applied-category, precision, RLS, admission uniqueness, and preservation
  of old classifications were caught and restored. A live-owner deletion test exposed
  a new-schema foreign-key timing defect, fixed with six deferred provenance checks;
  individual deletion of a referenced category still fails closed. Final validation:
  396 API tests in 31 files (68.86s), 77 browser tests (1.6m) against a fresh database
  with the final migrations, build/lint/typecheck pass, and independent Sol static
  review approves. Rebased on #65 for public-log protection, head `d6573bb` also passes
  396 API tests (30.38s), 89 browser tests (1.5m), and build/lint/typecheck. Fresh
  post-PR Sol review approves with no blockers and CI `34472289392` passes all
  checks. The review identified five redundant applied-value fields constrained
  to equal immutable proposal/run values. The simplification is published at
  `0223bf2`: five redundant fields and two FKs removed,
  with the applied tuple derived from immutable proposal values. Simplified-code
  validation passes 396 API tests (17.02s), 89 Playwright tests (1.8m) against a
  new migrated database, build/lint/typecheck, and Drizzle snapshot consistency.
  Bypassing the replacement state constraint produced the intended RED and was
  restored. Fresh independent Sol review approves the complete simplified diff
  and final documentation head; CI `34474315815` passes all checks. The PR is
  ready for founder review, subject to its dependency/migration-order notes. These are new
  foundation checks, not shipped-product defects. The separately confirmed initial
  inference overlap correction remains in 4.2.1; historical reruns belong to 4.2.4.
  Ready PR #73 (`26c92a1`), stacked on #72, adds inference admission in 4.2.1.
  Nine new real-PostgreSQL API tests cover post-lock lease expiry, overlap,
  independent owners, manual edits, metadata, and provider failures. Five source
  mutations produced intended RED and were restored: admission bypass, pre-lock
  lease evaluation, missing payload timestamp CAS, missing manual provenance
  advancement, and failure to release admission. Full validation passes 405 API
  tests / 32 files (37.15s), 89 Playwright tests (1.9m), build/lint/typecheck.
  The browser run used a fresh local database, real Clerk development/Sandbox,
  and the loopback model; temporary ports were restored and artifacts scrubbed.
  Fresh independent Sol review approves runtime and final documentation heads;
  CI `34475189732` and final-head CI `34476027162` pass all checks. An expired
  zero-progress response still requires a later page trigger, not same-mount
  recovery. Draft PR #74 (`26821a5`), stacked on #73, adds the server-only historical
  proposal/apply/rollback engine. Final local validation passes 429 API tests across
  33 files (108.90s), including 24 focused engine cases, plus build/lint/typecheck.
  Deliberate hash, post-lock lease, apply revision/timestamp, and rollback guard
  mutations produced intended failures and were restored. A survived revision
  mutation exposed a fixture gap: keeping its timestamp fixed now independently
  proves the revision check. Four independent rollback conflict cases were added.
  CI `34479703946` passes all checks. Fresh post-PR Sol review found a taxonomy
  race: rename or insertion can commit between taxonomy validation and the
  engine transaction commit. Current app code has no category edit/delete path,
  but raw owner SQL and future writers expose the stronger invariant's gap.
  A narrow database-enforced per-owner taxonomy lock correction is underway;
  the changed head requires focused concurrency regressions, complete validation,
  and renewed independent review. Full local browser validation remains pending.
  A captured-request cost guard, reviewed proposal
  view, and trusted operator entry remain production gates.
  Historical traversal is also required: repeated newest-first all-auto batches
  otherwise select the same already-corrected rows rather than reaching older history.
  No production categories have been
  overwritten by this work.
- Exact GPT review mapping requested by the user: Fable = Astra, Opus = Sol, Sonnet = Luna.
  Earlier #57/#60 reviews used actual Sonnet 5. Fresh independent Luna reviews approved
  #57's expanded head `367fff6`, #60's expanded head, and #62. Further changed heads
  require renewed validation and review.

## Additional battle-test evidence

- Integration `9e8f985` adds both #75 commits (`deb4840`, `aa2b2b5`) to
  `9db9ba3`. The shared-helper change replaces #67's duplicated credential
  guard while retaining all cleanup recovery cases. Full API: 430/430 across
  30 files (25.43s); full browser: 99/99 (4.3m), including #57's completion
  race, both #60 Clear cases, the real two-minute lost-cleanup-response retry,
  and the new early-exit provider teardown. Build and lint pass; temporary
  preview ports restored, artifacts scrubbed, integration worktree clean.
  This local-only branch is not a merge or replacement for rebased-head CI.
- #75's initial post-PR review found an additional sibling-spec failure-path
  leak. Corrected head `aa2b2b5` shares guarded teardown across both specs,
  passes fresh Luna static review, 388 API tests (14.60s), 8 focused browser
  checks (49.8s), and build/lint/typecheck. Moving provider removal after raw
  cleanup caused the intended failure; mutation-only recovery removed the real
  Item and correct order was restored. Isolated full browser had 90 passes and
  the known zero-import failure; the integration above resolves that dependency.
  Corrected-head CI `34482889701` passes all checks. No production Item touched.
- #74's taxonomy correction now passes 28 focused engine cases, including four
  actual PostgreSQL taxonomy interleavings, and fresh independent Sol static
  review finds no blockers. The uncommitted correction includes custom migration
  0027 and test helper simplification; full-suite validation and publication
  remain pending. This supersedes the earlier pending static-review status.

- Broader local integration at `9db9ba3` adds #60 (`9331b82` + `26c2a73`),
  #62 (`2249bff`), #69 (`b2e94fa` + `a8130c5`), #64 (`1d3c63b`), #70
  (`5d04f39`), and the already-deployed #59 (`7d19183`) to the import integration
  below. All 430 API tests in 30 files passed (24.37s), all 97 browser tests
  passed (4.0m), and production build/lint/typecheck passed. Both Spending Clear
  paths, History draft clearing, March 31 after February, webhook protections,
  and import/cleanup cases pass together. The real Clerk development/Sandbox run
  used the isolated local database; temporary ports were restored and artifact
  scrubbing verified. This evidence is for the assembled local tree, not CI on
  each future rebased head. #72 and the user's pending-reconciliation branch
  were deliberately not included; their migration journals remain separate.
- Local integration branch `7cffd1b` combines #67 (`a00e8fc`, including #66/#65),
  #57 (`bb66e30` + `367fff6`), #63 (`24a417e`), and #71 (`1a6a09e`). Both
  competing test insertions were retained when resolving the sole conflict; no
  behavioral assertion was weakened or skipped. All 397 API tests in 29 files
  passed (15.18s), all 93 browser tests passed (4.2m), and build/lint/typecheck
  passed. Real Clerk development and Plaid Sandbox used an isolated local DB.
  The competing-sync completion, duplicate Cancel, aged cleanup, real two-minute
  lost-acknowledgement retry, cursor-ready import recovery, and corrected spending
  drill all passed together. Temporary ports were restored and artifacts scrubbed.
  This closes local combined-workflow validation, not the submitted-head rebase/CI
  gates after founder merges. No open PR was merged or changed by this integration.
- A bounded synthetic benchmark on 2026-09-10 exercised the deployed PR #59
  classifier and default taxonomy through OpenRouter with
  `anthropic/claude-haiku-4.5`: 18 inputs, 18 returned classifications, 18 expected
  matches. Cases included explicit FSA reimbursement, brokerage contribution,
  grocery refund, Venmo rent, PayPal electronics, unknown debit, and an
  instruction-like bank description. The unknown debit received low confidence;
  the other 17 received high confidence. No production rows were sent or written.
  This small explicit-evidence sample does not establish real-account accuracy,
  calibration, or general prompt-injection resistance. A Transfer category does
  not itself exclude a row from cash flow; active transfer pairing does.
- Editing drafts and pressing Clear without Apply exposed a second reset path that
  the original query-key fix did not cover. Both corrected workflows now assert every
  control plus exact results after a subsequent Apply, not merely an empty URL.
- One full Spending run failed the known Plaid sandbox zero-transaction case, with
  72 other passes. An unchanged full rerun passed all 73, including the sandbox case.
  No test was weakened or skipped; endpoint probes showed Clerk/Plaid reachable.
  CI hit the same sandbox assertion once; rerunning only the failed job passed.
  Further recurrence in #57 prompted investigation: one of five local fresh-item
  runs reproduced zero persisted rows with complete status. Direct provider probes
  reproduced complete status with an empty cursor followed by rows on the next poll.
  This is now a diagnosed premature-completion defect, not an unexplained flake.
  Plaid's [cursor contract](https://plaid.com/docs/api/products/transactions/)
  says an empty `next_cursor` means transactions are not yet available. Synthetic
  probe Items were removed successfully; no real connection was touched.
  With #63, five consecutive real-Sandbox import tests passed without weakening
  the exact fixture checks, and the full local and CI browser suites passed.
- Newly shipped Upcoming renders a calendar and both lists without desktop overflow;
  displayed charge/deposit totals equal their respective displayed row sums. This
  is an internal display check, not independent validation of projected payment intent.
- September 10 live Chrome follow-up: all six displayed charges and both deposits
  occur exactly once on their stated calendar day, with matching labels and amounts.
  Monthly dates agree with the displayed previous occurrence, including August 31
  clamped to September 30; both fortnightly dates agree with 14-day increments.
  The visible page has no horizontal overflow at 1512 x 861. Next.js retains a hidden
  previous page in the DOM, so this check explicitly excludes non-visible elements.
  This is not a fresh institution comparison or a claim of live mobile validation.
- Production metadata confirms the custom domain serves the audited main commit.
  Public health is no-store, and protected routes redirect signed-out requests.
- All 12 protected mutation routes redirect unauthenticated empty-body POSTs to
  sign-in; an unsigned Plaid webhook returns 401. Fabricated identifiers were used,
  with no authenticated mutation or real financial-data changes.
- Read-only production catalog queries confirm all nine application tables force RLS,
  and both `cashlens_app` and `cashlens_owner` have neither superuser nor BYPASSRLS.
- September 10 recheck confirms those same nine table and two role boundaries.
  Public health returns 200 with no-store; signed-out History and categorize POST
  redirect to sign-in; unsigned webhook returns 401. HSTS, nosniff, and
  strict-origin-when-cross-origin headers are present. Main protection requires
  API, e2e, and lint/typecheck even for admins, with force pushes and deletion disabled.

## Completion gates

- [ ] Finish adversarial live workflow pass and record every confirmed failure.
- [ ] Validate each source-level concern with a focused failing test or stronger runtime evidence.
- [x] Resolve all correction PRs through the requested GPT review cycle, including
  renewed #57 coverage and PR67's final combined validation.
- [x] Verify the scoped production HTTP/auth, database role/RLS, and GitHub deployment
  gates without credential disclosure. Public-log correction #65 is merged;
  no authenticated cross-user production attack or destructive migration was performed.
- [x] Recheck #59 merge state and production activation: deployed, existing sensitive key configured, authenticated browser and read-only persistence checks pass. This does not certify category accuracy or implement safe reruns.
- [x] Publish this sanitized matrix for review; merge state is recorded above. PR #61 is merged; this document remains a bounded audit record.
