# Balance history error contract

Integration correction for PR102 after PR104: two rollback assertions still
required PostgreSQL's raw injected message. The scoped database boundary now
intentionally removes that message. Assert the generic error and SQLSTATE;
retain all exact account, balance, and snapshot rollback assertions.

Reuses the research and security contract in
`9.2-db-error-sanitization-prior-art.md`. No runtime, schema, dependency, or
authorization change. This is not a relaxation of the sanitization tests.

RED: full combined suite on main plus PR107 passed804/806. This test failed
because the raw message was absent; the other failure was a separate upgrade
test timeout. Focused/full validation and independent review pending.
